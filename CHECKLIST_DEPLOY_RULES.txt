═══════════════════════════════════════════════════════════
   🚨 CHECKLIST DEPLOY FIRESTORE RULES - 10/4/2026
═══════════════════════════════════════════════════════════

VẤN ĐỀ:
❌ Lỗi "Missing or insufficient permissions"
❌ Không thể gửi tin nhắn
❌ Online Status không hoạt động

NGUYÊN NHÂN:
⚠️  Firestore Rules chưa được deploy lên Firebase Server!

═══════════════════════════════════════════════════════════

HÀNH ĐỘNG NGAY:

[ ] 1. Mở Firebase Console
       → https://console.firebase.google.com
       → Login: kientrungvi0811@gmail.com
       → Chọn project: gen-lang-client-0050597412

[ ] 2. Vào Firestore Rules
       → Sidebar: Firestore Database
       → Tab: Rules

[ ] 3. Copy Rules mới
       → Mở file: firestore.rules
       → Copy TOÀN BỘ nội dung (Ctrl+A, Ctrl+C)

[ ] 4. Paste vào Console
       → Xóa hết rules cũ
       → Paste rules mới (Ctrl+V)

[ ] 5. Publish
       → Click nút "Publish" (màu xanh)
       → Đợi thông báo "Rules published successfully"

[ ] 6. Test
       → Hard refresh browser: Ctrl+Shift+R
       → Kiểm tra Console (F12): Không còn lỗi đỏ
       → Gửi tin nhắn thử
       → Xem Online Status

═══════════════════════════════════════════════════════════

KẾT QUẢ MONG ĐỢI:

✅ Không còn lỗi permissions
✅ Gửi tin nhắn được
✅ Online Status hoạt động
✅ Console sạch sẽ

═══════════════════════════════════════════════════════════

NẾU VẪN LỖI:

1. Đợi thêm 10-20 giây
2. Hard refresh nhiều lần
3. Clear cache: Ctrl+Shift+Delete
4. Thử Incognito mode

═══════════════════════════════════════════════════════════

📝 GHI CHÚ:
- Không dùng Firebase CLI (cần billing)
- Phải dùng Console (miễn phí)
- Rules đã đúng trong code, chỉ cần deploy!

═══════════════════════════════════════════════════════════
